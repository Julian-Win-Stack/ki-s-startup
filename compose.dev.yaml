services:
  receipt:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    environment:
      RECEIPT_DOCKER_MODE: dev
      RECEIPT_SERVER_WATCH: "${RECEIPT_SERVER_WATCH:-1}"
      RECEIPT_CSS_WATCH: "${RECEIPT_CSS_WATCH:-1}"
    user: "${LOCAL_UID:-1000}:${LOCAL_GID:-1000}"
    volumes:
      - .:/workspace/receipt
      - receipt-node-modules:/workspace/receipt/node_modules
      - ${RECEIPT_DOCKER_CODEX_SOURCE}:/mnt/host-auth/codex:ro
      - ${RECEIPT_DOCKER_AWS_SOURCE}:/mnt/host-auth/aws:ro
      - ${RECEIPT_DOCKER_GH_SOURCE}:/mnt/host-auth/gh:ro
      - ${RECEIPT_DOCKER_GITCONFIG_SOURCE}:/mnt/host-auth/gitconfig:ro
      - ${RECEIPT_DOCKER_GIT_CREDENTIALS_SOURCE}:/mnt/host-auth/git-credentials:ro
      - ${RECEIPT_DOCKER_SSH_SOURCE}:/mnt/host-auth/ssh:ro
      - ${RECEIPT_DOCKER_SSH_AUTH_SOURCE}:${RECEIPT_DOCKER_SSH_AUTH_TARGET}

volumes:
  receipt-node-modules:
